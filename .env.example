# ==========================================
# RaceVision AI Backend Environment Settings
# ==========================================

# Server ports and bindings
PORT=8000
HOST=0.0.0.0

# CORS origins policies configuration (set to production domains in prod)
CORS_ORIGINS=*

# Optional Data/Model paths overrides
MODELS_DIR=models
PROCESSED_DIR=data/processed
